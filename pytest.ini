[pytest]
addopts = -ra --ignore=tests/distributed
norecursedirs = .git logs venv
markers =
    unit: quick CPU/GPU-agnostic tests
    integration: single-process GPU tests
    gpu: requires at least one CUDA device
    perf: performance tests (may be slow)